name: Test au push sur develop

on:
  push: 
    branches: 
      - develop
      
jobs:
  test:
    name: Test
    runs-on: ubuntu-20.04
     
    steps:
      - name: Install
        run: npm install

      - name: Testing
        run: npm run test
